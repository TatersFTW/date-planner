/* ==========================================================================
   firebase-config.js: your project's keys, and nothing else.

   Cloud sync is optional. With this left as null, the app works exactly as
   before: you swap links manually from the Sync page. Fill this in once (see
   README.md, "Turning on cloud sync") and both of you get automatic sync
   the next time you open the site, because you share the same deployed code.

   These values identify which free Firebase project to talk to. They are
   not secret: they are visible in your site's source to anyone anyway, the
   same way a shop's street address is public. What keeps your plans private
   is the password, which never leaves your browsers, and the fact that
   Firebase only ever stores encrypted text that it can't read.
   ========================================================================== */

window.DP_FIREBASE_CONFIG = null;

/* Paste your project's config here instead, then redeploy. It looks like:

window.DP_FIREBASE_CONFIG = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "...",
  appId: "..."
};

*/
